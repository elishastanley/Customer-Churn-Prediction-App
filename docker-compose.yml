version: '3'  # You can keep this line, although it's optional in the latest Docker Compose versions
services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    command: streamlit run app.py --server.port 5000 --server.headless true
